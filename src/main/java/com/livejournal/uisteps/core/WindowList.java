package com.livejournal.uisteps.core;

import java.util.Set;
import org.openqa.selenium.WebDriver;

/**
 *
 * @author ASolyankin
 */
public class WindowList {
/*    
    private final WebDriver driver;
    private final Set<String> windowList;
    private final String currentWindow;

    public WindowList(WebDriver driver) {
        this.driver = driver;
        windowList = driver.getWindowHandles();
        currentWindow = windowList.
    }
    
    swit
    
    
    
     public void user_click_TOS_Link() throws InterruptedException {
        verify.that(getUrl());
        on(CreateAccountPage.class).getCreateAccountForm().getTosRusLink().click();
        WebDriver driver = getCurrentBrowser().getDriver();
         Set<String> dsadf = driver.getWindowHandles();
        for (String winHandle : driver.getWindowHandles()) {
            driver.switchTo()..window(winHandle);
            verify.that("Click on TOS Link 1  " + driver.getCurrentUrl());
        }
    }
    */
}
